# Происхождение — AlfaGen PII Gateway

Журнал происхождения кода, библиотек, моделей и внешних материалов. Не подделывать.

## Разрешённые инструменты

- Предоставленный организаторами DeepSeek (DeepSeek-V4-Flash-0731) через провайдер `alfagen` в Kilo Code.
- VS Code, Kilo Code.
- Источник: [S6] — ограничение инструментов: только предоставленный DeepSeek, VS Code, Kilo Code.

## Внешние материалы

- Мастер-промпт `master_prompt.md` (редакция 2) — предоставлен пользователем; содержит требования, уточнения и проектные решения. Происхождение указано.
- Допустимость внешнего планирующего документа: отдельного ответа организаторов о заранее подготовленных промптах нет. Это не подтверждённое разрешение и не отдельно установленный запрет. Требование письменного разрешения до начала работы было авторской интерпретацией первой версии, а не цитатой организатора; в редакции 2 оно не является автоматическим стоп-фактором.

## Код

- Ранее созданного кода в workspace нет (на 22.09.2026).
- Весь код создан в рамках этого проекта (G1–G8) с фиксацией происхождения.
- G8: созданы `scripts/package_submission.py`, `Dockerfile`, `compose.yaml`, `docs/architecture.md`, `docs/limitations.md`, `docs/evidence-checklist.md`; обновлены `docs/requirements.md`, `docs/scorecard.md`, `docs/benchmark.md`, `docs/provenance.md`, `memory.md`.

## Библиотеки и модели

- Runtime BERT: ранее отдельно разрешён внутри pipeline; не используется в текущей реализации (детекторы rule-based).
- Используемые библиотеки (из pyproject.toml): fastapi, uvicorn, pydantic, pydantic-settings, pyyaml, cryptography, redis; dev: pytest, httpx. Версии фиксируются при установке; lockfile не создан.

## Статус официальной спецификации

- Официальный API Appendix A: MISSING. Официальный адаптер помечается UNVERIFIED до реальной сверки. Отсутствие схемы не блокирует независимое ядро, demo API, детекторы, токенизацию, Vault, round-trip и тесты.

## Статус на G8 (22.09.2026)

- Source ZIP: `artifacts/alfagen_source.zip` (sha256 `2a8ffb9be9969a2b26416510f197084b7ea633fdf98029a5cc63f2aea9f59e4b`). Содержит только исходники/конфиги/документацию; без зависимостей, моделей, баз, ПД, секретов и мусора (R70).
- Чистая установка из ZIP проверена: 223 passed, exit 0; smoke `ROUND_TRIP_OK: True` (R71).
- Публичный endpoint: BLOCKED (нет инфраструктуры/credentials); предоставлен локальный запуск и контейнерный путь (Dockerfile/compose.yaml).

## Примечание о checksum

SHA256 в этом файле — checksum архива на момент сборки. Так как документация входит в архив, правка документации после сборки меняет checksum архива. Авторитетное значение checksum финального архива записано в итоговом отчёте сдачи (delivery report).
